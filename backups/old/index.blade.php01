<!DOCTYPE html>
<html>
<head>
    <title>Borrowers</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-4">

<div class="d-flex justify-content-between mb-3">
    <h2>Borrowers</h2>

    <a href="{{ route('borrowers.create') }}" class="btn btn-primary">
        + Add Borrower
    </a>
</div>

<h2>Borrowers List</h2>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Name</th>
            <th>Contact</th>
            <th>Email</th>
<th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach($borrowers as $b)
            <tr>
                <td>{{ $b->firstname }} {{ $b->lastname }}</td>
                <td>{{ $b->contact_no }}</td>
                <td>{{ $b->email }}</td>
<td><a href="{{ route('borrowers.show', $b->id) }}"
   class="btn btn-info btn-sm">
    <i class="fa fa-eye"></i>
</a>

<a href="{{ route('borrowers.edit', $b->id) }}"
   class="btn btn-warning btn-sm">
    <i class="fa fa-edit"></i>
</a>

<form action="{{ route('borrowers.destroy', $b->id) }}"
      method="POST"
      style="display:inline;">
    @csrf
    @method('DELETE')

    <button type="submit"
            class="btn btn-danger btn-sm"
            onclick="return confirm('Delete this borrower?')">
        <i class="fa fa-trash"></i>
    </button>
</form></td>
            </tr>
        @endforeach
    </tbody>
</table>

</body>
</html>
