@extends('layouts.erp')

@section('title', 'Settings')

@section('content')

<div class="p-6">

    <!-- HEADER -->
    <div class="mb-6">
        <h1 class="text-3xl font-bold text-gray-800">Settings</h1>
        <p class="text-gray-500">Configure system preferences and organisation settings</p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">

        <!-- CHAMA PROFILE -->
        <div class="bg-white shadow rounded-xl p-6">

            <h2 class="text-xl font-bold mb-4">Organisation Profile</h2>

            <form method="POST" action="#">
                @csrf

                <div class="space-y-4">

                    <div>
                        <label class="text-sm text-gray-600">Organisation Name</label>
                        <input type="text"
                               class="w-full border rounded-lg px-3 py-2"
                               placeholder="e.g. Kisii Chama">
                    </div>

                    <div>
                        <label class="text-sm text-gray-600">Email</label>
                        <input type="email"
                               class="w-full border rounded-lg px-3 py-2">
                    </div>

                    <div>
                        <label class="text-sm text-gray-600">Phone</label>
                        <input type="text"
                               class="w-full border rounded-lg px-3 py-2">
                    </div>

                    <button class="bg-blue-600 text-white px-4 py-2 rounded-lg">
                        Save Changes
                    </button>

                </div>
            </form>

        </div>

        <!-- SYSTEM SETTINGS -->
        <div class="bg-white shadow rounded-xl p-6">

            <h2 class="text-xl font-bold mb-4">System Settings</h2>

            <div class="space-y-4">

                <div class="flex items-center justify-between">
                    <span class="text-gray-700">Enable Notifications</span>
                    <input type="checkbox" class="scale-125">
                </div>

                <div class="flex items-center justify-between">
                    <span class="text-gray-700">Auto Loan Reminders</span>
                    <input type="checkbox" class="scale-125">
                </div>

                <div class="flex items-center justify-between">
                    <span class="text-gray-700">Dark Mode (future)</span>
                    <input type="checkbox" class="scale-125">
                </div>

            </div>

        </div>

    </div>

</div>

@endsection
